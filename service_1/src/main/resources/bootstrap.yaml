spring:
  profiles:
    active: dev
  application:
    name: serviceA
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: '127.0.0.1:8848'
      config:
        enabled: true
        group: openStore
#        prefix: ${spring.application.name}
        server-addr: '127.0.0.1:8848'
        file-extension: 'yaml'
#        endpoint: '127.0.0.1:6002'
server:
  port: 6002